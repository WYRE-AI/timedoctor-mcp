{
  "name": "@wyre-ai/timedoctor-mcp",
  "version": "0.1.0",
  "description": "MCP server for Time Doctor's employee time-tracking API - companies, users, projects, tasks, and productivity stats",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "timedoctor-mcp": "./dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/WYRE-AI/timedoctor-mcp"
  },
  "homepage": "https://github.com/WYRE-AI/timedoctor-mcp",
  "scripts": {
    "build": "tsup",
    "prebuild": "npm run clean",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "start:http": "MCP_TRANSPORT=http node dist/http.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "timedoctor",
    "time-doctor",
    "mcp",
    "msp",
    "time-tracking",
    "productivity",
    "model-context-protocol"
  ],
  "author": "WYRE Technology",
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^7.0.0",
    "@semantic-release/github": "^12.0.8",
    "@semantic-release/npm": "^13.0.0",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^2.1.8",
    "semantic-release": "^25.0.9",
    "tsup": "^8.0.0",
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "zod": "4.3.6"
  }
}
